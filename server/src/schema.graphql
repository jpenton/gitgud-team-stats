# import * from '../generated/graphql-schema/schema.graphql'

type Query {
  player(id: ID!): Player
  team(id: ID!): Team
  teams: [Team]!
}

type Mutation {
  createPlayer(bnet: String!, discord: String!, role: Role!): Player!
}
