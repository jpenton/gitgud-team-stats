# import * from '../generated/graphql-schema/schema.graphql'

type Team {
  id: ID!
  name: String!
  players: [Player!]
}

type Query {
  player(id: ID!): Player
  team(id: ID!): Team
  teams: [Team!]
}
