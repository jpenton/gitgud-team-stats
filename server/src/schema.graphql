# import * from '../generated/graphql-schema/schema.graphql'

type Team {
  id: ID!
  name: String!
  players: [Player!]
  slug: String!
}

type Query {
  player(id: ID!): Player
  team(id: ID, slug: String): Team
  teams: [Team!]
}
